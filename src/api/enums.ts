export enum HttpMethod {
  GET = 'get',
  POST = 'post',
  PUT = 'put',
  PATCH = 'patch',
  DELETE = 'delete',
}

export enum LocalStorageKey {
  ACCOUNT_TOKEN = 'cb_at',
}

export enum AccountType {
  TUTOR = 'tutor',
  PARENT = 'parent',
}

export enum GenderType {
  MALE = 'male',
  FEMALE = 'female',
}
